build/
*.bin
*.elf
*.map
__pycache__/
.DS_Store

.env
.tmp/

# Harness artefacts — never publish. docs/graph.* is the primer's cross-project
# knowledge graph and carries material from unrelated (including client) work;
# .remotes.json carries push topology and operator identity.
docs/graph.json
docs/graph.html
.remotes.json

# Harness scaffolding — kept on disk, not published. It points at a private
# local toolchain (~/.primer) and is not part of this project.
.claude/
.githooks/
opencode.json
.primer-version.json
AGENTS.md
CLAUDE.md
docs/atoms/
docs/RECALL.md
